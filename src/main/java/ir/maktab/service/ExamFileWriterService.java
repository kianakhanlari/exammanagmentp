package ir.maktab.service;

import ir.maktab.model.Question;

import java.io.BufferedWriter;
import java.io.FileWriter;
import java.io.IOException;
import java.util.List;

public class ExamFileWriterService {
/*    private  final ExamService examService;

    public ExamFileWriterService(ExamService examService) {
        this.examService = examService;
    }

    public void files(Long examId) {
        List<Question> qu=examService.findQuestionsByExamId();
        String path = "exam.txt";
        try (BufferedWriter writer = new BufferedWriter(new FileWriter(path))){
            writer.write(text);
        } catch (IOException ex) {
            throw new RuntimeException(ex);
        }

    }*/
}
